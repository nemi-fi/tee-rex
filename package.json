{
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "install-deps": "pnpm i",
    "build": "docker build -t tee-rex --platform linux/amd64 ."
  },
  "devDependencies": {
    "turbo": "2.5.4"
  }
}
